"""The per-lane data modules (docs/runner-conversion-plan.md §3): one
importable table set per lane, read by that lane's runner and by every
gate that used to parse the runner's text. win is the first; mac, ios,
android and linux join as their runners convert."""
