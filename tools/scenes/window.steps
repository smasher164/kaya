# 640x400 is off the 540x330 default, so an ignored size request cannot
# pass by luck. Desktop-only: the phones' systems own surface geometry.
expect label#0 "window probe"
expect_title "window probe"
expect_window_size 640x400
