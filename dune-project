(lang dune 3.0)
; The OCaml build: the binding library lives in bindings/ocaml, the
; guests in guests/ocaml. Everything else in the repo is other
; languages' business — the root dune file scopes traversal.
