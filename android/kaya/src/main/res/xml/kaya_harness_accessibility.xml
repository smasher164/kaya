<?xml version="1.0" encoding="utf-8"?>
<!--
  canRetrieveWindowContent and flagRetrieveInteractiveWindows are what
  let the harness read DocumentsUI, a different app: without them
  getWindows() returns nothing and the failure looks like the picker
  never appeared. No packageNames filter, deliberately, for the same
  reason.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeAllMask"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRetrieveInteractiveWindows|flagReportViewIds"
    android:canRetrieveWindowContent="true"
    android:description="@string/kaya_harness_a11y_description"
    android:notificationTimeout="0" />
