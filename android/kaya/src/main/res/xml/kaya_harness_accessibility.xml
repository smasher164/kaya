<?xml version="1.0" encoding="utf-8"?>
<!--
  The harness's accessibility service config. canRetrieveWindowContent
  and flagRetrieveInteractiveWindows are what let it read DocumentsUI,
  which is a different app: without them getWindows() returns nothing and
  the failure looks like the picker never appeared.

  No packageNames filter, deliberately — the whole point is to see a
  window this app does not own.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeAllMask"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRetrieveInteractiveWindows|flagReportViewIds"
    android:canRetrieveWindowContent="true"
    android:description="@string/kaya_harness_a11y_description"
    android:notificationTimeout="0" />
