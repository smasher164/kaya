[package]
name = "kaya-derive"
version = "0.0.0"
edition = "2024"

[lib]
proc-macro = true

# Compile-time only: none of this reaches a runtime binary.
[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full"] }
