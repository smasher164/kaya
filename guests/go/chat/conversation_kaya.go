// Code generated by kaya-gen; DO NOT EDIT.
// Regenerate with `go generate ./...` (tools/gen-guests.py checks freshness).

package chat

import (
	"iter"

	kaya "dev.kaya/bindings/go"
)

// ConversationCollection declares the record collection; the struct is the
// schema.
func ConversationCollection(tx *kaya.Tx) kaya.RecordCollection[string, Conversation] {
	return kaya.CollectionOf[string, Conversation](tx)
}

// ConversationPatch opens a named-setter patch on one entry; each setter
// records one update_field — a patch is recorded writes, never a
// diff.
func ConversationPatch(c kaya.RecordCollection[string, Conversation], tx *kaya.Tx, key string) conversationPatchBuilder {
	return conversationPatchBuilder{c.Patch(tx, key)}
}

type conversationPatchBuilder struct {
	p kaya.RecordPatch[string, Conversation]
}

func (p conversationPatchBuilder) Name(v string) conversationPatchBuilder {
	p.p.Set(func(t *Conversation) *string { return &t.Name }, v)
	return p
}

func (p conversationPatchBuilder) Preview(v string) conversationPatchBuilder {
	p.p.Set(func(t *Conversation) *string { return &t.Preview }, v)
	return p
}

func (p conversationPatchBuilder) Unread(v string) conversationPatchBuilder {
	p.p.Set(func(t *Conversation) *string { return &t.Unread }, v)
	return p
}

// The row surface: one token per wire field, and the template
// constructors that consume them.
type conversationRow struct {
	t *kaya.Tpl
	c kaya.RecordCollection[string, Conversation]
}

func (r conversationRow) Name() kaya.Field[string] { return kaya.FieldAt[string](0) }

func (r conversationRow) Preview() kaya.Field[string] { return kaya.FieldAt[string](1) }

func (r conversationRow) Unread() kaya.Field[string] { return kaya.FieldAt[string](2) }

func (r conversationRow) Row(body func()) kaya.Node { return r.t.Row(body) }

func (r conversationRow) Column(body func()) kaya.Node { return r.t.Column(body) }

func (r conversationRow) Label(f kaya.Field[string]) kaya.Node { return r.c.Label(r.t, f) }

func (r conversationRow) Heading(f kaya.Field[string]) kaya.Node { return r.c.Heading(r.t, f) }

func (r conversationRow) Caption(f kaya.Field[string]) kaya.Node { return r.c.Caption(r.t, f) }

func (r conversationRow) Image(f kaya.Field[[]byte]) kaya.Node { return r.c.Image(r.t, f) }

func (r conversationRow) Checkbox(f kaya.Field[bool], onToggle func(*kaya.Tx, string, bool)) kaya.Node {
	return r.c.Checkbox(r.t, f, onToggle)
}

func (r conversationRow) Button(f kaya.Field[string], onClick func(*kaya.Tx, string)) kaya.Node {
	return r.c.Button(r.t, f, onClick)
}

func (r conversationRow) Spacer() kaya.Node { return r.t.Spacer() }

func (r conversationRow) Entry(onChange func(*kaya.Tx, string, string)) kaya.Node {
	return r.c.Entry(r.t, onChange)
}

func (r conversationRow) TextareaRich(f kaya.Field[kaya.Document], onEdit func(*kaya.Tx, string, kaya.Edit), onFormat func(*kaya.Tx, string, kaya.Format)) kaya.Node {
	return r.c.TextareaRich(r.t, f, onEdit, onFormat)
}

func (r conversationRow) DatePicker(f kaya.Field[kaya.Date], onDate func(*kaya.Tx, string, kaya.Date)) kaya.Node {
	return r.c.DatePicker(r.t, f, onDate)
}

func (r conversationRow) TimePicker(f kaya.Field[kaya.Time], onTime func(*kaya.Tx, string, kaya.Time)) kaya.Node {
	return r.c.TimePicker(r.t, f, onTime)
}

func (r conversationRow) Slider(min, max float64, f kaya.Field[float64], onChange func(*kaya.Tx, string, float64)) kaya.Node {
	return r.c.Slider(r.t, min, max, f, onChange)
}

// The template props: the const flavors write through the recorder,
// the sourced ones through the typed collection, as the constructors
// above do.
func (r conversationRow) SetGrow(n kaya.Node, weight float64) { r.t.SetGrow(n, weight) }

func (r conversationRow) SetStep(n kaya.Node, step float64) { r.t.SetStep(n, step) }

func (r conversationRow) SetTickSpacing(n kaya.Node, spacing float64) { r.t.SetTickSpacing(n, spacing) }

func (r conversationRow) SetA11yID(n kaya.Node, id string) { r.t.SetA11yID(n, id) }

func (r conversationRow) A11yID(n kaya.Node, f kaya.Field[string]) { r.c.A11yID(r.t, n, f) }

func (r conversationRow) SetA11yLabel(n kaya.Node, label string) { r.t.SetA11yLabel(n, label) }

func (r conversationRow) A11yLabel(n kaya.Node, f kaya.Field[string]) { r.c.A11yLabel(r.t, n, f) }

func (r conversationRow) SetA11yHint(n kaya.Node, hint string) { r.t.SetA11yHint(n, hint) }

func (r conversationRow) A11yHint(n kaya.Node, f kaya.Field[string]) { r.c.A11yHint(r.t, n, f) }

func (r conversationRow) SetHelp(n kaya.Node, text string) { r.t.SetHelp(n, text) }

func (r conversationRow) Help(n kaya.Node, f kaya.Field[string]) { r.c.Help(r.t, n, f) }

func (r conversationRow) SetPlaceholder(n kaya.Node, text string) { r.t.SetPlaceholder(n, text) }

func (r conversationRow) Placeholder(n kaya.Node, f kaya.Field[string]) { r.c.Placeholder(r.t, n, f) }

func (r conversationRow) SetHref(n kaya.Node, url string) { r.t.SetHref(n, url) }

func (r conversationRow) Href(n kaya.Node, f kaya.Field[string]) { r.c.Href(r.t, n, f) }

func (r conversationRow) SetAccepts(n kaya.Node, kinds ...string) { r.t.SetAccepts(n, kinds...) }

func (r conversationRow) SetFill(n kaya.Node, on bool)          { r.t.SetFill(n, on) }
func (r conversationRow) SetAlign(n kaya.Node, mode kaya.Align) { r.t.SetAlign(n, mode) }
func (r conversationRow) SetFilled(n kaya.Node, tint kaya.Tint) { r.t.SetFilled(n, tint) }

func (r conversationRow) SetColumnsAuto(n kaya.Node, minWidth float64) {
	r.t.SetColumnsAuto(n, minWidth)
}

func (r conversationRow) SetWrap(n kaya.Node, on bool) { r.t.SetWrap(n, on) }

func (r conversationRow) SetSubmits(n kaya.Node, on bool) { r.t.SetSubmits(n, on) }

func (r conversationRow) Draggable(n kaya.Node) kaya.TplDragRef { return r.t.Draggable(n) }

func (r conversationRow) SetDropTarget(n kaya.Node, ops ...kaya.Op) { r.t.SetDropTarget(n, ops...) }

func (r conversationRow) SetRole(n kaya.Node, role kaya.Role) { r.t.SetRole(n, role) }

func (r conversationRow) SetInset(n kaya.Node, pad float64) { r.t.SetInset(n, pad) }

// ConversationRows opens the record template as a for statement: the For is
// minted here, the chain declares the table, and the loop body runs
// once with the typed row surface (exact-index tokens, no probes);
// stamping is the core's replay.
func ConversationRows(tx *kaya.Tx, c kaya.RecordCollection[string, Conversation]) *conversationRowsFor {
	return &conversationRowsFor{tx.Rows(c.Handle()), c}
}

// The embedded *kaya.Rows carries Widget(); the two chain methods
// are re-spelled only to keep the typed All() reachable through them.
type conversationRowsFor struct {
	*kaya.Rows
	c kaya.RecordCollection[string, Conversation]
}

func (f *conversationRowsFor) Columns(titles []string, sort kaya.Sort) *conversationRowsFor {
	f.Rows.Columns(titles, sort)
	return f
}

func (f *conversationRowsFor) OnSort(fn func(*kaya.Tx, uint32)) *conversationRowsFor {
	f.Rows.OnSort(fn)
	return f
}

func (f *conversationRowsFor) All() iter.Seq[conversationRow] {
	return func(yield func(conversationRow) bool) {
		for row := range f.Rows.All() {
			if !yield(conversationRow{t: row.Tpl, c: f.c}) {
				return
			}
		}
	}
}
