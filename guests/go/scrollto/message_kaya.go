// Code generated by kaya-gen; DO NOT EDIT.
// Regenerate with `go generate ./...` (tools/gen-guests.py checks freshness).

package scrollto

import (
	"iter"

	kaya "dev.kaya/bindings/go"
)

// MessageCollection declares the record collection; the struct is the
// schema.
func MessageCollection(tx *kaya.Tx) kaya.RecordCollection[string, Message] {
	return kaya.CollectionOf[string, Message](tx)
}

// MessagePatch opens a named-setter patch on one entry; each setter
// records one update_field — a patch is recorded writes, never a
// diff.
func MessagePatch(c kaya.RecordCollection[string, Message], tx *kaya.Tx, key string) messagePatchBuilder {
	return messagePatchBuilder{c.Patch(tx, key)}
}

type messagePatchBuilder struct {
	p kaya.RecordPatch[string, Message]
}

func (p messagePatchBuilder) Text(v string) messagePatchBuilder {
	p.p.Set(func(t *Message) *string { return &t.Text }, v)
	return p
}

// The row surface: one token per wire field, and the template
// constructors that consume them.
type messageRow struct {
	t *kaya.Tpl
	c kaya.RecordCollection[string, Message]
}

func (r messageRow) Text() kaya.Field[string] { return kaya.FieldAt[string](0) }

func (r messageRow) Row(body func()) kaya.Node { return r.t.Row(body) }

func (r messageRow) Column(body func()) kaya.Node { return r.t.Column(body) }

func (r messageRow) Label(f kaya.Field[string]) kaya.Node { return r.c.Label(r.t, f) }

func (r messageRow) Heading(f kaya.Field[string]) kaya.Node { return r.c.Heading(r.t, f) }

func (r messageRow) Caption(f kaya.Field[string]) kaya.Node { return r.c.Caption(r.t, f) }

func (r messageRow) Image(f kaya.Field[[]byte]) kaya.Node { return r.c.Image(r.t, f) }

func (r messageRow) Checkbox(f kaya.Field[bool], onToggle func(*kaya.Tx, string, bool)) kaya.Node {
	return r.c.Checkbox(r.t, f, onToggle)
}

func (r messageRow) Entry(onChange func(*kaya.Tx, string, string)) kaya.Node {
	return r.c.Entry(r.t, onChange)
}

func (r messageRow) TextareaRich(f kaya.Field[kaya.Document], onEdit func(*kaya.Tx, string, kaya.Edit), onFormat func(*kaya.Tx, string, kaya.Format)) kaya.Node {
	return r.c.TextareaRich(r.t, f, onEdit, onFormat)
}

func (r messageRow) DatePicker(f kaya.Field[kaya.Date], onDate func(*kaya.Tx, string, kaya.Date)) kaya.Node {
	return r.c.DatePicker(r.t, f, onDate)
}

func (r messageRow) TimePicker(f kaya.Field[kaya.Time], onTime func(*kaya.Tx, string, kaya.Time)) kaya.Node {
	return r.c.TimePicker(r.t, f, onTime)
}

func (r messageRow) Slider(min, max float64, f kaya.Field[float64], onChange func(*kaya.Tx, string, float64)) kaya.Node {
	return r.c.Slider(r.t, min, max, f, onChange)
}

// The template props: the const flavors write through the recorder,
// the sourced ones through the typed collection, as the constructors
// above do.
func (r messageRow) SetGrow(n kaya.Node, weight float64) { r.t.SetGrow(n, weight) }

func (r messageRow) SetStep(n kaya.Node, step float64) { r.t.SetStep(n, step) }

func (r messageRow) SetTickSpacing(n kaya.Node, spacing float64) { r.t.SetTickSpacing(n, spacing) }

func (r messageRow) SetA11yID(n kaya.Node, id string) { r.t.SetA11yID(n, id) }

func (r messageRow) A11yID(n kaya.Node, f kaya.Field[string]) { r.c.A11yID(r.t, n, f) }

func (r messageRow) SetA11yLabel(n kaya.Node, label string) { r.t.SetA11yLabel(n, label) }

func (r messageRow) A11yLabel(n kaya.Node, f kaya.Field[string]) { r.c.A11yLabel(r.t, n, f) }

func (r messageRow) SetA11yHint(n kaya.Node, hint string) { r.t.SetA11yHint(n, hint) }

func (r messageRow) A11yHint(n kaya.Node, f kaya.Field[string]) { r.c.A11yHint(r.t, n, f) }

func (r messageRow) SetHelp(n kaya.Node, text string) { r.t.SetHelp(n, text) }

func (r messageRow) Help(n kaya.Node, f kaya.Field[string]) { r.c.Help(r.t, n, f) }

func (r messageRow) SetPlaceholder(n kaya.Node, text string) { r.t.SetPlaceholder(n, text) }

func (r messageRow) Placeholder(n kaya.Node, f kaya.Field[string]) { r.c.Placeholder(r.t, n, f) }

func (r messageRow) SetHref(n kaya.Node, url string) { r.t.SetHref(n, url) }

func (r messageRow) Href(n kaya.Node, f kaya.Field[string]) { r.c.Href(r.t, n, f) }

func (r messageRow) SetAccepts(n kaya.Node, kinds ...string) { r.t.SetAccepts(n, kinds...) }

func (r messageRow) SetFill(n kaya.Node, on bool)          { r.t.SetFill(n, on) }
func (r messageRow) SetAlign(n kaya.Node, mode kaya.Align) { r.t.SetAlign(n, mode) }
func (r messageRow) SetFilled(n kaya.Node, tint kaya.Tint) { r.t.SetFilled(n, tint) }

func (r messageRow) SetColumnsAuto(n kaya.Node, minWidth float64) { r.t.SetColumnsAuto(n, minWidth) }

func (r messageRow) SetWrap(n kaya.Node, on bool) { r.t.SetWrap(n, on) }

func (r messageRow) SetSubmits(n kaya.Node, on bool) { r.t.SetSubmits(n, on) }

func (r messageRow) Draggable(n kaya.Node) kaya.TplDragRef { return r.t.Draggable(n) }

func (r messageRow) SetDropTarget(n kaya.Node, ops ...kaya.Op) { r.t.SetDropTarget(n, ops...) }

func (r messageRow) SetRole(n kaya.Node, role kaya.Role) { r.t.SetRole(n, role) }

func (r messageRow) SetInset(n kaya.Node, pad float64) { r.t.SetInset(n, pad) }

// MessageRows opens the record template as a for statement: the For is
// minted here, the chain declares the table, and the loop body runs
// once with the typed row surface (exact-index tokens, no probes);
// stamping is the core's replay.
func MessageRows(tx *kaya.Tx, c kaya.RecordCollection[string, Message]) *messageRowsFor {
	return &messageRowsFor{tx.Rows(c.Handle()), c}
}

// The embedded *kaya.Rows carries Widget(); the two chain methods
// are re-spelled only to keep the typed All() reachable through them.
type messageRowsFor struct {
	*kaya.Rows
	c kaya.RecordCollection[string, Message]
}

func (f *messageRowsFor) Columns(titles []string, sort kaya.Sort) *messageRowsFor {
	f.Rows.Columns(titles, sort)
	return f
}

func (f *messageRowsFor) OnSort(fn func(*kaya.Tx, uint32)) *messageRowsFor {
	f.Rows.OnSort(fn)
	return f
}

func (f *messageRowsFor) All() iter.Seq[messageRow] {
	return func(yield func(messageRow) bool) {
		for row := range f.Rows.All() {
			if !yield(messageRow{t: row.Tpl, c: f.c}) {
				return
			}
		}
	}
}
