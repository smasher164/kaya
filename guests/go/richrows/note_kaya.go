// Code generated by kaya-gen; DO NOT EDIT.
// Regenerate with `go generate ./...` (tools/gen-guests.py checks freshness).

package richrows

import (
	"iter"

	kaya "dev.kaya/bindings/go"
)

// NoteCollection declares the record collection; the struct is the
// schema.
func NoteCollection(tx *kaya.Tx) kaya.RecordCollection[string, Note] {
	return kaya.CollectionOf[string, Note](tx)
}

// NotePatch opens a named-setter patch on one entry; each setter
// records one update_field — a patch is recorded writes, never a
// diff.
func NotePatch(c kaya.RecordCollection[string, Note], tx *kaya.Tx, key string) notePatchBuilder {
	return notePatchBuilder{c.Patch(tx, key)}
}

type notePatchBuilder struct {
	p kaya.RecordPatch[string, Note]
}

func (p notePatchBuilder) Title(v string) notePatchBuilder {
	p.p.Set(func(t *Note) *string { return &t.Title }, v)
	return p
}

func (p notePatchBuilder) Body(v kaya.Document) notePatchBuilder {
	p.p.Set(func(t *Note) *kaya.Document { return &t.Body }, v)
	return p
}

// The row surface: one token per wire field, and the template
// constructors that consume them.
type noteRow struct {
	t *kaya.Tpl
	c kaya.RecordCollection[string, Note]
}

func (r noteRow) Title() kaya.Field[string] { return kaya.FieldAt[string](0) }

func (r noteRow) Body() kaya.Field[kaya.Document] { return kaya.FieldAt[kaya.Document](1) }

func (r noteRow) Row(body func()) kaya.Node { return r.t.Row(body) }

func (r noteRow) Column(body func()) kaya.Node { return r.t.Column(body) }

func (r noteRow) Label(f kaya.Field[string]) kaya.Node { return r.c.Label(r.t, f) }

func (r noteRow) Heading(f kaya.Field[string]) kaya.Node { return r.c.Heading(r.t, f) }

func (r noteRow) Caption(f kaya.Field[string]) kaya.Node { return r.c.Caption(r.t, f) }

func (r noteRow) Image(f kaya.Field[[]byte]) kaya.Node { return r.c.Image(r.t, f) }

func (r noteRow) Checkbox(f kaya.Field[bool], onToggle func(*kaya.Tx, string, bool)) kaya.Node {
	return r.c.Checkbox(r.t, f, onToggle)
}

func (r noteRow) TextareaRich(f kaya.Field[kaya.Document], onEdit func(*kaya.Tx, string, kaya.Edit), onFormat func(*kaya.Tx, string, kaya.Format)) kaya.Node {
	return r.c.TextareaRich(r.t, f, onEdit, onFormat)
}

func (r noteRow) DatePicker(f kaya.Field[kaya.Date], onDate func(*kaya.Tx, string, kaya.Date)) kaya.Node {
	return r.c.DatePicker(r.t, f, onDate)
}

func (r noteRow) TimePicker(f kaya.Field[kaya.Time], onTime func(*kaya.Tx, string, kaya.Time)) kaya.Node {
	return r.c.TimePicker(r.t, f, onTime)
}

func (r noteRow) Slider(min, max float64, f kaya.Field[float64], onChange func(*kaya.Tx, string, float64)) kaya.Node {
	return r.c.Slider(r.t, min, max, f, onChange)
}

// The template props: the const flavors write through the recorder,
// the sourced ones through the typed collection, as the constructors
// above do.
func (r noteRow) SetGrow(n kaya.Node, weight float64) { r.t.SetGrow(n, weight) }

func (r noteRow) SetStep(n kaya.Node, step float64) { r.t.SetStep(n, step) }

func (r noteRow) SetTickSpacing(n kaya.Node, spacing float64) { r.t.SetTickSpacing(n, spacing) }

func (r noteRow) SetA11yID(n kaya.Node, id string) { r.t.SetA11yID(n, id) }

func (r noteRow) A11yID(n kaya.Node, f kaya.Field[string]) { r.c.A11yID(r.t, n, f) }

func (r noteRow) SetA11yLabel(n kaya.Node, label string) { r.t.SetA11yLabel(n, label) }

func (r noteRow) A11yLabel(n kaya.Node, f kaya.Field[string]) { r.c.A11yLabel(r.t, n, f) }

func (r noteRow) SetA11yHint(n kaya.Node, hint string) { r.t.SetA11yHint(n, hint) }

func (r noteRow) A11yHint(n kaya.Node, f kaya.Field[string]) { r.c.A11yHint(r.t, n, f) }

func (r noteRow) SetHelp(n kaya.Node, text string) { r.t.SetHelp(n, text) }

func (r noteRow) Help(n kaya.Node, f kaya.Field[string]) { r.c.Help(r.t, n, f) }

func (r noteRow) SetPlaceholder(n kaya.Node, text string) { r.t.SetPlaceholder(n, text) }

func (r noteRow) Placeholder(n kaya.Node, f kaya.Field[string]) { r.c.Placeholder(r.t, n, f) }

func (r noteRow) SetHref(n kaya.Node, url string) { r.t.SetHref(n, url) }

func (r noteRow) Href(n kaya.Node, f kaya.Field[string]) { r.c.Href(r.t, n, f) }

func (r noteRow) SetAccepts(n kaya.Node, kinds ...string) { r.t.SetAccepts(n, kinds...) }

func (r noteRow) SetFill(n kaya.Node, on bool) { r.t.SetFill(n, on) }

func (r noteRow) SetColumnsAuto(n kaya.Node, minWidth float64) { r.t.SetColumnsAuto(n, minWidth) }

func (r noteRow) SetWrap(n kaya.Node, on bool) { r.t.SetWrap(n, on) }

func (r noteRow) Draggable(n kaya.Node) kaya.TplDragRef { return r.t.Draggable(n) }

func (r noteRow) SetDropTarget(n kaya.Node, ops ...kaya.Op) { r.t.SetDropTarget(n, ops...) }

func (r noteRow) SetRole(n kaya.Node, role kaya.Role) { r.t.SetRole(n, role) }

func (r noteRow) SetInset(n kaya.Node, pad float64) { r.t.SetInset(n, pad) }

// NoteRows opens the record template as a for statement: the For is
// minted here, the chain declares the table, and the loop body runs
// once with the typed row surface (exact-index tokens, no probes);
// stamping is the core's replay.
func NoteRows(tx *kaya.Tx, c kaya.RecordCollection[string, Note]) *noteRowsFor {
	return &noteRowsFor{tx.Rows(c.Handle()), c}
}

// The embedded *kaya.Rows carries Widget(); the two chain methods
// are re-spelled only to keep the typed All() reachable through them.
type noteRowsFor struct {
	*kaya.Rows
	c kaya.RecordCollection[string, Note]
}

func (f *noteRowsFor) Columns(titles []string, sort kaya.Sort) *noteRowsFor {
	f.Rows.Columns(titles, sort)
	return f
}

func (f *noteRowsFor) OnSort(fn func(*kaya.Tx, uint32)) *noteRowsFor {
	f.Rows.OnSort(fn)
	return f
}

func (f *noteRowsFor) All() iter.Seq[noteRow] {
	return func(yield func(noteRow) bool) {
		for row := range f.Rows.All() {
			if !yield(noteRow{t: row.Tpl, c: f.c}) {
				return
			}
		}
	}
}
