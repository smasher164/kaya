/target
result
.direnv
/third_party/
tools/winui-bindgen/target
tools/kaya-bindgen/target
tools/win/dialogprobe/target
tools/win/clipprobe/target
# Android probe/helper gradle debris (the original clipprobe commit
# tracked its build tree by mistake; kept out from here on).
tools/android/clipprobe/.gradle
tools/android/clipprobe/app/build
tools/android/cliphelper/.gradle
tools/android/cliphelper/app/build
# Generated by build.sh beside them, with the CRLF cmd.exe needs.
tools/win/dialogprobe/dialogprobe.cmd
tools/win/clipprobe/clipprobe.cmd
__pycache__/
# ocamlopt and ghc write intermediates beside the source when run
# in-place (the validation scripts copy to target/ first, but ad-hoc
# runs don't).
/guests/ocaml/*.cmi
/guests/ocaml/*.cmx
/guests/ocaml/*.o
/guests/haskell/*.hi
/guests/haskell/*.o
/target-linux/
/android/.gradle/
/android/**/build/
/android/*/src/main/jniLibs/
/android/.kotlin/
# The picker probe is a SEPARATE gradle build (tools/android/pickerprobe),
# deliberately outside /android so it can never affect what the lane
# ships — which also puts it outside the four rules above. Its outputs
# are 480-odd files and they nearly went into a commit.
/tools/android/pickerprobe/.gradle/
/tools/android/pickerprobe/**/build/
/tools/android/pickerprobe/.kotlin/
guests/csharp/bin/
guests/csharp/obj/
tools/kaya-csgen/bin/
tools/kaya-csgen/obj/
tools/kaya-swift-gen/.build/
/_build/
/_build-linux/
guests/haskell/dist-newstyle/
# go build without -o (and friends) drop a binary named after the
# package in the cwd; a stray one at the root almost reached a commit
# once. One entry per guest scene name.
/todos
/entry
/gallery
/milestone2
/reorder
/feed
/kaya-gen
# Written per-run by tools/android/run-emulator.sh (the interpreter's
# build-id marker); never committed.
/android/kaya/generated
